<!DOCTYPE html>
<html lang="en">
<head>
    <title>Welcome to the Digital Gallery</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            background: url('/images/splash-bg.jpg') no-repeat center center fixed;
            background-size: cover;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
            animation: fadeIn 1.5s ease-in-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            color: white;
            font-size: 1.5rem;
            z-index: 9999;
        }

        .loading-image {
            width: 300px;
            height: 200px;
            border-radius: 10px;
            object-fit: cover;
            margin-top: 20px;
        }

        .splash-container {
            background: rgba(255, 255, 255, 0.85);
            padding: 30px;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            animation: scaleUp 1s ease-in-out;
            display: none;
        }

        @keyframes scaleUp {
            from { transform: scale(0.8); }
            to { transform: scale(1); }
        }

        h1 {
            font-family: 'Fredoka One', cursive;
            color: #ff8c00;
        }

        h4, h3 {
            font-family: 'Comic Sans MS', cursive;
            color: #333;
        }

        form {
            margin-top: 20px;
        }

        input {
            padding: 10px;
            border-radius: 5px;
            border: 2px solid #ff8c00;
            width: 80%;
            font-size: 1rem;
        }

        button {
            margin-top: 10px;
            background-color: #ff8c00;
            color: white;
            padding: 10px 15px;
            border: none;
            cursor: pointer;
            border-radius: 20px;
            font-size: 1rem;
            transition: background 0.3s, transform 0.2s;
        }

        button:hover {
            background: #ff6500;
            transform: scale(1.1);
        }
    </style>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const loadingScreen = document.querySelector(".loading-screen");
            const splashContainer = document.querySelector(".splash-container");
            const loadingImage = document.querySelector(".loading-image");
            
            // Fetch random image from uploads/A folder
            fetch("/random-image")
                .then(response => response.json())
                .then(data => {
                    loadingImage.src = data.imagePath;
                });

            setTimeout(() => {
                loadingScreen.style.display = "none";
                splashContainer.style.display = "block";
            }, 3000);
        });
    </script>
</head>
<body>

    <div class="splash-container">
        <h1>Welcome to the Digital Gallery</h1>
        <hr>
        <h4>“Yaadon Ka Album – A Collection of Priceless Moments”</h4>
        <hr>
        <h3>Exclusively for Batch 2024-25</h3>
        <h3>Class of 12th B Grade</h3>
        <h3>All Your Cherished Memories</h3>
        <hr>
        <form action="/get-name" method="POST">
            <input type="text" name="username" placeholder="Enter your name" required>
            <button type="submit">Let's Goooo</button>
        </form>
    </div>
</body>
</html>
